{
  "version": 2,
  "name": "rcdb-api",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "api",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
